// Installera dependencies
const prompt = require('prompt-sync')()

/*const fs = require("fs")
const path = require("path")

function jumpFolder(folderPaths){
    folderPaths.forEach(folderPath => {
        const results =  fs.readdirSync(folderPath)
        const folders = results.filter(res =>  fs.lstatSync(path.resolve(folderPath, res)).isDirectory())
        const innerFolderPaths = folders.map(folder => path.resolve(folderPath,folder))
        if(innerFolderPaths.length === 0){
            return
        }
        innerFolderPaths.forEach(innerFolder => console.log(innerFolder))
        jumpFolder(innerFolderPaths)
    })


}
jumpFolder([path.resolve(__dirname, 
    "C:\\Users\\emil_\\OneDrive\\Desktop\\frontend1-javascriptzzz\\")])
*/



// C:\\Users\\emil_\\OneDrive\\Desktop\\frontend1-javascriptzzz\\


let number = "1"
console.log(parseFloat(number))


parseInt


















// console.debug(process)
// Deklarera variabler
/* const myName = prompt("Who am I? ")
let myAge = prompt("How old am I? ")
let beverage = prompt("What kind of beverage do I like? ")
console.log("Hello world, my name is " + myName + ".")
// console.log(`I'm ${myAge} years old.`)
console.log(`And I like ${beverage}`)


const birthDay = prompt("Is it my birthday y/n? ").trim().toLowerCase()
if(birthDay == "y") {
    let newAge = Number(myAge)
    newAge += 1
    console.log("Hurray, now I'm " + (newAge)) 
} else {
    console.log("Ok")
}
console.log(process.argv)}
*/




























/* switch(app){
    case 01-variabler:
        
}*/